# Unfinished/raw assets
Maken Brother unrefined logo.jpeg

# Old drafts
Maken Brothers Website (5).html

# Project notes / briefs
maken-brothers-build-brief.md
DEPLOY.md

# Netlify local state
.netlify/

# Test tooling
# NOTE: package.json must NOT live in the repo root - the deploy host treats a
# root package.json as "this is a Node project, run a build" and the build fails.
# Test tooling lives in tests/ for that reason.
node_modules/
test-shots/

# Agent tooling
.claude/
skills-lock.json

# OS
.DS_Store
Thumbs.db
