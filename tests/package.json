{
  "name": "makenbrothers-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Smoke test tooling. Lives here, not in the repo root, because a root package.json makes the deploy host switch from serving static files to running a Node build.",
  "type": "module",
  "scripts": {
    "test": "node smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
